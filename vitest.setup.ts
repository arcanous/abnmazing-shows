import { afterAll, afterEach, beforeAll } from 'vitest'

// Minimal setup file, add more global setup as needed
beforeAll(() => {
  // any setup before all tests
})

afterAll(() => {
  // any cleanup after all tests
})

afterEach(() => {
  // cleanup after each test
}) 
